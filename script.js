// Good Luck 💪🏾
const sentence = "my name is rayyan";
const sentence = "my name is rayyan";
const words = mySentence.split(" ");

const sentence = "my name is rayyan";
const words = sentence.split(" ");

for (let i = 0; i < words.length; i++) {
    words[i] = words[i][0].toUpperCase() + words[i].substr(1);
}

const sentence = "my name is rayyn";
const words = sentence.split(" ");

for (let i = 0; i < words.length; i++) {
    words[i] = words[i][0].toUpperCase() + words[i].substr(1);
}

words.join(" ");

